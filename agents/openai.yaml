interface:
  display_name: "Character Sheet Prompt Builder"
  short_description: "Build consistent three- or four-panel character prompts"
  default_prompt: "Use $build-character-sheet-prompt to analyze my character image and create a production-ready character sheet prompt using the default three-panel layout unless I request the optional exact 90-degree side-view panel."

